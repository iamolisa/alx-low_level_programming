#include "main.h"

/**
 * add - adds a and b and returns the result
 * @a: a number to be added to b
 * @b: a number to be added to a
 * Return: sumation of a and b
 */
int add(int a, int b)
{
	return (a + b);
}
